{
  "name": "anki-mockup",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Phase-0 visual prototype — Notion/Linear-inspired redesign with cream + dark palette. Fake data only, no backend.",
  "scripts": {
    "dev": "vite dev --host 0.0.0.0 --port 5174",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5174",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.53.3",
    "@sveltejs/vite-plugin-svelte": "^5.1.0",
    "playwright": "^1.57.0",
    "svelte": "^5.53.5",
    "svelte-check": "^4.2.2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
